# Story 7.3 — Ordenação de Produtos no Catálogo

**Epic:** EPIC-7 — UX Galeria & Catálogo  
**Status:** Done ✅  
**Validado por:** Pax (@po) — 2026-06-17 | GO (10/10)  
**Prioridade:** Média — catálogo de 426 produtos exibido em ordem de API sem controle do usuário  
**Criado:** 2026-06-17  
**Owner:** @dev  
**Executor:** Dex (@dev)  
**Quality Gate:** @qa

---

## Story

**As a** usuária do catálogo LBG,  
**I want** poder ordenar os produtos da grade por critérios relevantes,  
**so that** eu encontre rapidamente produtos que precisam de atenção (revisão, fotos ruins) ou navegue alfabeticamente.

---

## Contexto

A grade de produtos em `src/app/catalogo/page.tsx` exibe os 426 SKUs na ordem retornada pela API (`GET /api/produtos`). Não há controle de ordenação. Com o crescimento do catálogo, isso dificulta:

- Priorizar revisão de fotos (quem tem `qualidade_foto: ruim` primeiro?)
- Navegar por nome (A-Z para busca rápida)
- Identificar produtos com mais ou menos fotos

---

## Acceptance Criteria

### Controle de Sort (`src/app/catalogo/page.tsx`)

- [ ] AC1: Adicionar estado `sortMode` com tipo `'api' | 'nome-az' | 'qualidade-asc' | 'fotos-desc' | 'revisao-first'` — default: `'api'`
- [ ] AC2: Adicionar dropdown `<select>` na topbar ao lado dos botões Grade/Lista com as opções:
  - "Padrão" (ordem da API)
  - "Nome A→Z"
  - "Qualidade ↑ (piores primeiro)"
  - "Mais fotos"
  - "Revisão pendente primeiro"
- [ ] AC3: A lista `unique` (produtos filtrados) é ordenada conforme `sortMode` antes de renderizar
- [ ] AC4: Sort "Qualidade ↑ (piores primeiro)" usa ordem: `ruim → regular → boa → excelente → sem qualidade` — útil para curadoria
- [ ] AC5: Sort "Revisão pendente primeiro" move produtos com `precisa_revisao: true` para o topo, mantendo sub-ordem por nome A-Z
- [ ] AC6: Sort "Mais fotos" ordena por `p.imagens?.length ?? 1` decrescente
- [ ] AC7: Sort persiste durante filtragem (aplicar sort após aplicar filtros)
- [ ] AC8: Sort é resetado junto com `clearFilters()` — volta para `'api'`

### Visual

- [ ] AC9: Dropdown estilizado com `className="btn btn-outline btn-sm"` para consistência visual com os outros controles da topbar
- [ ] AC10: Label do dropdown mostra opção selecionada (comportamento nativo do `<select>`)

### Quality Gates

- [ ] AC11: `npm run lint` passa sem novos erros
- [ ] AC12: `npm run typecheck` passa sem erros — `sortMode` tipado corretamente
- [ ] AC13: Sem regressão nos filtros existentes (categoria, qualidade, revisão, busca)

---

## Arquivos a Modificar

| Arquivo | Mudança |
|---------|---------|
| `src/app/catalogo/page.tsx` | Adicionar estado `sortMode`, função `applySortMode()`, `<select>` na topbar |

---

## Notas de Implementação

A função de sort opera sobre o array `unique` (já filtrado):

```ts
type SortMode = 'api' | 'nome-az' | 'qualidade-asc' | 'fotos-desc' | 'revisao-first';

const QUAL_SORT: Record<string, number> = { ruim:0, regular:1, boa:2, excelente:3 };

function applySortMode(list: Produto[], mode: SortMode): Produto[] {
  switch (mode) {
    case 'nome-az':      return [...list].sort((a,b) => a.nome_produto.localeCompare(b.nome_produto));
    case 'qualidade-asc': return [...list].sort((a,b) => (QUAL_SORT[a.qualidade_foto??''] ?? 4) - (QUAL_SORT[b.qualidade_foto??''] ?? 4));
    case 'fotos-desc':   return [...list].sort((a,b) => (b.imagens?.length??1) - (a.imagens?.length??1));
    case 'revisao-first': return [...list].sort((a,b) => {
      if (a.precisa_revisao === b.precisa_revisao) return a.nome_produto.localeCompare(b.nome_produto);
      return a.precisa_revisao ? -1 : 1;
    });
    default: return list;
  }
}

const sorted = applySortMode(unique, sortMode);
```

---

## Progresso

- [x] Adicionar estado `sortMode` e tipo `SortMode`
- [x] Implementar `applySortMode()`
- [x] Adicionar `<select>` na topbar
- [x] Integrar sort no pipeline de filtragem
- [x] `clearFilters()` reseta `sortMode` para `'api'`
- [x] `npm run lint` + `npm run typecheck`
- [x] @qa review

---

## QA Results

**Revisor:** Quinn (@qa) | **Data:** 2026-06-17 | **Gate:** PASS ✅

### Verificação de ACs

- AC1: `SortMode` type (line 12) + `useState<SortMode>('api')` (line 438) ✓
- AC2: `<select>` na topbar com 5 opções corretas (lines 486-497) ✓
- AC3: `const unique = applySortMode(filtered, sortMode)` (line 464) — sort após filtro ✓
- AC4: `QUAL_SORT = {ruim:0, regular:1, boa:2, excelente:3}` → piores primeiro ✓
- AC5: `revisao-first` — `precisa_revisao ? -1 : 1` + sub-ordem localeCompare ✓
- AC6: `fotos-desc` — `(b.imagens?.length??1) - (a.imagens?.length??1)` ✓
- AC7: Sort aplicado sobre `filtered` → persiste com filtros ativos ✓
- AC8: `clearFilters()` inclui `setSortMode('api')` ✓
- AC9/10: `<select className="btn btn-outline btn-sm">` — visual consistente ✓
- AC11: lint sem novos erros (pre-existing `<img>` warnings) ✓
- AC12: `npx tsc --noEmit` — zero output ✓
- AC13: Filtros existentes inalterados — sem regressão ✓
