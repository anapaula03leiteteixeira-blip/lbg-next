# Story 5.1 — Upgrade de Prompts SEO em generate-copies.ts

**Epic:** EPIC-5 — Copies SEO Multi-Plataforma (Claude-Direct)  
**Status:** Ready for Review  
**Prioridade:** Alta — desbloqueio de todo o EPIC-5  
**Criado:** 2026-06-07  
**Owner:** @dev  
**Executor:** Dex (@dev)  
**Quality Gate:** @qa

---

## Contexto

O script `scripts/generate-copies.ts` já existe (EPIC-1 Story 1.4) com toda a infraestrutura de batch processing, rate limiting, UPSERT no Supabase e flags CLI (`--dry-run`, `--force`, `--estimate`, `--sku`, `--plataforma`). O que falta é inteligência nos prompts: os prompts atuais em `buildPrompt()` são genéricos e não incorporam as regras SEO de cada plataforma.

Esta story atualiza `buildPrompt()` e `contextoBase()` com prompts refinados baseados em pesquisa de algoritmos de cada plataforma (Atlas/2026-06-07).

**Arquivo a modificar:** `scripts/generate-copies.ts` (projeto lbg-next)

---

## User Story

Como operador do sistema, quero executar `npx ts-node --project tsconfig.scripts.json scripts/generate-copies.ts --dry-run --sku LBG100IPANEMA` e receber copies SEO que respeitam os limites e padrões de cada plataforma (keywords no início do título Shopee, bullet points estruturados Amazon, ficha técnica para ML, etc.), para que os listings gerados maximizem visibilidade e conversão.

---

## Acceptance Criteria

### scripts/generate-copies.ts

- [x] AC1: Função `contextoBase()` inclui parágrafo de contexto da marca La Bella Griffe antes dos dados do produto
- [x] AC2: Chamada Claude usa `system` message com instrução de especialista em SEO e-commerce BR
- [x] AC3: Prompt **Mercado Livre** — título ≤60 chars, fórmula `[Produto] [material/dimensão] [cor] [diferencial]`, sem artigos, sem emojis. Descrição foca em converter (não ranquear): responde objeções, destaca diferenciais e dimensões. `ficha_tecnica_hints`: array com 3 atributos-chave.
- [x] AC4: Prompt **Shopee** — título ≤58 chars com keyword principal nos primeiros 15 chars (peso 3.2×). Tom acessível, sem keyword stuffing, cada keyword aparece uma vez. Descrição: custo-benefício + qualidade.
- [x] AC5: Prompt **Amazon** — título 150-200 chars começando com "La Bella Griffe". Bullet points: 5 bullets estruturados com prefixo em maiúsculas (`MATERIAL PREMIUM:`, `DIMENSÕES EXATAS:`, `INSTALAÇÃO FÁCIL:`, `DESIGN ITALIANO:`, `GARANTIA E QUALIDADE:`). `backend_keywords`: string corrida sem vírgula para campo de busca oculto.
- [x] AC6: Prompt **Leroy Merlin** — título 80-120 chars com contexto de reforma. Descrição ≤280 chars (limite da plataforma), técnica, com dimensões e compatibilidade.
- [x] AC7: Prompt **Madeira Madeira** — título 80-120 chars com apelo de design + ambiente. Descrição ≤2000 chars, aspiracional + prática.
- [x] AC8: `COPY_LIMITS` inline no script atualizado: `shopee.titulo → 58`, `leroy_merlin.descricao → 280`, `leroy_merlin.titulo → 120`, `madeira_madeira.titulo → 120`
- [x] AC9: Query de eligibilidade expandida: inclui produtos SEM `descricao_marketing` (usa metadados: nome + categoria + material + cor + tags). Não bloqueia mais em `.not("descricao_marketing", "is", null)` nem `.neq("descricao_marketing", "")`
- [ ] AC10: `--dry-run --sku LBG100IPANEMA` executa sem erro e imprime copies para as 5 plataformas
- [x] AC11: `npm run typecheck` passa sem erros após a mudança
- [x] AC12: Output JSON de cada plataforma tem os campos esperados (`titulo`, `descricao`, `palavras_chave` + `bullets` onde aplicável + `ficha_tecnica_hints` para ML + `backend_keywords` para Amazon)

### Correções arquiteturais (revisão @architect — impacto 129→426 produtos)

- [x] AC13: `ProdutoDB.descricao_marketing` tipado como `string | null` em **ambos os arquivos** (`scripts/generate-copies.ts` e `src/app/api/copies/generate/route.ts`)
- [x] AC14: `contextoBase()` em **ambos os arquivos** tem null guard para `descricao_marketing` — linha deve ser `p.descricao_marketing ? \`Descrição marketing: ${p.descricao_marketing}\` : null` (consistente com outros campos opcionais)
- [x] AC15: `src/app/api/copies/generate/route.ts` — guard `if (!p.descricao_marketing)` removido (linhas 103-105): endpoint aceita produtos sem descrição marketing e gera copy com metadados disponíveis; `buildPrompt()` atualizado com os mesmos prompts melhorados (AC3-7)
- [x] AC16: `src/types/index.ts` — `COPY_LIMITS` atualizado com valores corretos: `shopee.titulo → 58`, `leroy_merlin.titulo → 120`, `leroy_merlin.descricao → 280`, `madeira_madeira.titulo → 120` (source of truth usado pela API route)

---

## Especificação dos Prompts

### Sistema (system message para todas as chamadas)
```
Você é um especialista em SEO para e-commerce brasileiro com foco em produtos de banheiro
(cubas, torneiras, pastilhas, acessórios sanitários).
Gere copies que maximizem visibilidade nas buscas E convertam vendas.
Responda APENAS com JSON válido, sem texto adicional.
```

### Contexto da marca (adicionar no início de contextoBase)
```
Marca: La Bella Griffe — louças e metais sanitários de origem italiana.
Fabricados com porcelana vitrificada de alta resistência. Posicionamento: qualidade
premium a preço acessível. Garantia de fábrica. Distribuição nacional.
```

### buildPrompt — Mercado Livre
```
{contextoBase}

Gere listing para Mercado Livre Brasil. JSON:
{
  "titulo": "≤60 chars — [Produto] [material] [cor/acabamento] — sem artigos, sem emojis",
  "descricao": "3-4 frases convertendo quem já está na página: benefícios concretos, dimensões exatas, compatibilidade, facilidade de instalação. Tom direto e confiante.",
  "palavras_chave": ["5 termos que compradores digitam no ML — do mais específico ao mais amplo"],
  "ficha_tecnica_hints": ["3 atributos-chave para preencher na ficha técnica ML"]
}
```

### buildPrompt — Shopee
```
{contextoBase}

Gere listing para Shopee Brasil. JSON:
{
  "titulo": "≤58 chars — keyword principal NOS PRIMEIROS 15 CHARS (obrigatório, têm peso 3.2x no algoritmo). Sem keyword stuffing. Tom acessível.",
  "descricao": "2-3 frases curtas — custo-benefício, qualidade, o que o comprador ganha. Tom acessível.",
  "palavras_chave": ["5 termos simples de alta intenção, como comprador Shopee buscaria"]
}
```

### buildPrompt — Amazon
```
{contextoBase}

Gere listing para Amazon Brasil. JSON:
{
  "titulo": "150-200 chars — começa com 'La Bella Griffe', inclui produto + material + dimensão + 2 diferenciais",
  "bullets": [
    "MATERIAL PREMIUM: <louça/porcelana vitrificada, resistência, acabamento>",
    "DIMENSÕES EXATAS: <medidas para instalação sem surpresas>",
    "INSTALAÇÃO FÁCIL: <compatibilidade, instruções, o que vem na caixa>",
    "DESIGN ITALIANO: <estética, linha, combinação com outros produtos La Bella>",
    "GARANTIA E QUALIDADE: <durabilidade, marca, suporte pós-venda>"
  ],
  "descricao": "HTML com <p><ul><li> ≤2000 chars. Responde perguntas do comprador, quebra objeções, conduz à compra.",
  "palavras_chave": ["5-8 termos incluindo variações técnicas e sinônimos"],
  "backend_keywords": "string corrida com sinônimos variações de busca sem vírgula sem repetição"
}
```

### buildPrompt — Leroy Merlin
```
{contextoBase}

Gere listing para Leroy Merlin Brasil (perfil: reformador, profissional ou DIY). JSON:
{
  "titulo": "80-120 chars — produto + material + dimensão + contexto de uso em reforma",
  "descricao": "≤280 chars (LIMITE DA PLATAFORMA) — specs técnicas, compatibilidade, contexto de reforma, facilidade de instalação",
  "palavras_chave": ["5 termos com foco em reforma, instalação, compatibilidade"],
  "ficha_tecnica_hints": ["3 atributos técnicos prioritários para Leroy"]
}
```

### buildPrompt — Madeira Madeira
```
{contextoBase}

Gere listing para MadeiraMadeira (perfil: decoração + reforma + entrega rápida). JSON:
{
  "titulo": "80-120 chars — produto + estilo/design + cor + aplicação no ambiente",
  "descricao": "≤2000 chars — design, qualidade visual, dimensões, como transforma o banheiro. Tom aspiracional mas direto.",
  "palavras_chave": ["5 termos com foco em design, ambiente, decoração de banheiro"]
}
```

---

## Notas Técnicas

**COPY_LIMITS após mudança:**
```typescript
const COPY_LIMITS = {
  amazon:          { titulo: 200, bullets: { count: 5, chars: 255 }, descricao: 2000 },
  mercado_livre:   { titulo: 60,                                      descricao: 4000 },
  shopee:          { titulo: 58,                                       descricao: 3000 },
  leroy_merlin:    { titulo: 120, bullets: { count: 10, chars: 200 }, descricao: 280  },
  madeira_madeira: { titulo: 120,                                      descricao: 2000 },
};
```

**Query expandida — incluir produtos sem descricao_marketing:**
```typescript
let query = sb
  .from('produtos')
  .select('sku, nome_produto, categoria, subcategoria, cor_dominante, material_aparente, tags, descricao_marketing, descricao_tecnica')
  .not('nome_produto', 'is', null);  // apenas exclui registros sem nome
// Remover .not("descricao_marketing", "is", null) e .neq("descricao_marketing", "")
```

**CopyRaw — adicionar campos novos:**
```typescript
interface CopyRaw {
  titulo?: string;
  bullets?: string[];
  descricao?: string;
  palavras_chave?: string[];
  ficha_tecnica_hints?: string[];   // ML + Leroy
  backend_keywords?: string;         // Amazon
}
```

---

## File List

| Arquivo | Projeto | Ação |
|---------|---------|------|
| `scripts/generate-copies.ts` | lbg-next | MODIFIED — contextoBase (marca + null guard), buildPrompt (5 plataformas), COPY_LIMITS inline, query eligibilidade (AC9), CopyRaw interface, system message, ProdutoDB tipo |
| `src/app/api/copies/generate/route.ts` | lbg-next | MODIFIED — removido guard 422 descricao_marketing, ProdutoDB tipo, contextoBase null guard, buildPrompt prompts atualizados (mesmos AC3-7), system message, CopyRaw novos campos |
| `src/types/index.ts` | lbg-next | MODIFIED — COPY_LIMITS: shopee.titulo→58, leroy_merlin.titulo→120, leroy_merlin.descricao→280, madeira_madeira.titulo→120 |

---

## Notas Técnicas Adicionais — Correções Arquiteturais (@architect)

### AC13-14 — Tipo e null guard (ambos os arquivos)

```typescript
// ProdutoDB — alterar em ambos os arquivos:
descricao_marketing: string | null;  // era: string

// contextoBase() — alterar linha do descricao_marketing:
p.descricao_marketing ? `Descrição marketing: ${p.descricao_marketing}` : null,
// era: `Descrição marketing: ${p.descricao_marketing}` (sem guard — imprimia "null")
```

### AC15 — Remover guard 422 em src/app/api/copies/generate/route.ts

```typescript
// REMOVER estas linhas (103-105):
if (!p.descricao_marketing) {
  return NextResponse.json({ error: "Produto sem descricao_marketing — execute o script enrich-marketing primeiro" }, { status: 422 });
}
// Após remoção, produtos sem descricao_marketing geram copy via metadados disponíveis
```

Também atualizar `buildPrompt()` neste arquivo com os prompts melhorados (AC3-7 — mesma spec do script).

### AC16 — COPY_LIMITS em src/types/index.ts (linhas 116-122)

```typescript
// DE (valores incorretos):
shopee:          { titulo: 120, descricao: 3000 },
leroy_merlin:    { titulo: 100, bullets: { count: 10, chars: 200 }, descricao: 1000 },
madeira_madeira: { titulo: 150, descricao: 2000 },

// PARA (valores corretos):
shopee:          { titulo: 58,  descricao: 3000 },
leroy_merlin:    { titulo: 120, bullets: { count: 10, chars: 200 }, descricao: 280  },
madeira_madeira: { titulo: 120, descricao: 2000 },
```

Este arquivo é o source of truth — a API route (`src/app/api/copies/generate/route.ts`) importa `COPY_LIMITS` de `@/types`, então a correção aqui resolve automaticamente os limites da UI.

---

## Definition of Done

- [ ] `--dry-run --sku {SKU}` retorna copies válidos para as 5 plataformas
- [x] Shopee título ≤58 chars com keyword nos primeiros 15
- [x] Amazon título começa com "La Bella Griffe" e tem 150-200 chars
- [x] Leroy Merlin descrição ≤280 chars
- [x] ML tem `ficha_tecnica_hints`; Amazon tem `backend_keywords`
- [x] Produtos SEM `descricao_marketing` são incluídos (script: sem filtro; API: sem 422)
- [x] Botão "Re-gerar" na UI (`/admin/copies`) funciona para SKU sem `descricao_marketing`
- [x] `npm run typecheck` passa sem erros nos 3 arquivos modificados
- [x] Story 5.2 desbloqueada

---

## QA Results

**Gate:** ✅ PASS with CONCERNS
**Revisor:** @qa (Quinn) — 2026-06-08
**ACs verificados:** 15/16 (AC10 depende de ambiente externo — N/A para gate)

### Concerns (não-bloqueantes)

**[MEDIUM] Leroy Merlin bullets vs ficha_tecnica_hints:**
`COPY_LIMITS.leroy_merlin` mantém `bullets: { count: 10, chars: 200 }` mas o novo prompt usa `ficha_tecnica_hints`. Resultado: `bullets: []` gravado no banco para Leroy Merlin. Funcionalmente não quebra — tech debt para Story 5.2 ou sprint seguinte.

**[LOW] max_tokens: 1200 pode truncar Amazon:**
Amazon prompt pede título + 5 bullets + HTML ≤2000 + backend_keywords. Monitorar em produção. Considerar 1600 para Amazon na Story 5.2.

**[LOW] Tipo PromiseFulfilledResult desatualizado em route.ts:128:**
Cast não inclui `ficha_tecnica_hints`/`backend_keywords`. Runtime correto, tipo TypeScript incompleto. Cosmético.

**Aprovado para push via @devops.**
