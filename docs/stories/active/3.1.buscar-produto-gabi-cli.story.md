# Story 3.1 — Comando *buscar-produto no Agente Gabi

**Epic:** EPIC-3 — Integração Gabi ↔ lbg-next  
**Status:** InProgress  
**Prioridade:** Alta — habilita EPIC-5 (Ubersuggest + copies) e Story 3.3  
**Criado:** 2026-06-07  
**Owner:** @dev  
**Executor:** Dex (@dev)  
**Quality Gate:** @architect (api_contract_validation, auth_token_check)

---

## Contexto

O agente Gabi (social media La Bella Griffe) hoje busca dados de produtos via PDFs estáticos do Google Drive. O lbg-next já expõe `/api/gabi/produto/[sku]` e `/api/gabi/produtos` com autenticação via `X-API-Key` (EPIC-1 concluído). Esta story adiciona ao agente Gabi dois comandos CLI que consultam esses endpoints diretamente, substituindo a dependência de PDFs por dados dinâmicos em tempo real.

**Chave de API disponível:** `GABI_API_KEY=999A810C945D47F9F72B8BB6AECA58735AC9B2A3`  
**Header:** `X-API-Key: {GABI_API_KEY}`  
**Base URL:** `https://lbg-next.vercel.app`

---

## User Story

Como agente Gabi, quero executar `*buscar-produto LBG100IPANEMA` e receber imediatamente os dados completos do produto do catálogo lbg-next (nome, cor, qualidade_foto, image_url, descricao_marketing, tags), para gerar conteúdo sem precisar abrir PDFs ou aguardar arquivos do Drive.

---

## Acceptance Criteria

- [ ] AC1: Comando `*buscar-produto {SKU}` existe no agente Gabi e está documentado na saudação
- [ ] AC2: `*buscar-produto LBG100IPANEMA` retorna em formato legível: nome, categoria, cor_dominante, qualidade_foto, image_url (Cloudinary), descricao_marketing, tags
- [ ] AC3: Resposta inclui lista de `imagens[]` com url, angulo e qualidade_foto de cada foto
- [ ] AC4: Se produto não encontrado → mensagem clara: "Produto {SKU} não encontrado no catálogo. Verifique o SKU e tente novamente."
- [ ] AC5: Autenticação via `X-API-Key: 999A810C945D47F9F72B8BB6AECA58735AC9B2A3` no header (não exposta ao usuário)
- [ ] AC6: Comando `*listar-produtos --categoria=cuba` retorna lista de produtos filtrados por categoria
- [ ] AC7: `*listar-produtos` sem filtro retorna todos os produtos (paginado se >50)
- [ ] AC8: Se lbg-next offline ou erro 5xx → mensagem: "Catálogo temporariamente indisponível. Tente novamente em instantes."
- [ ] AC9: Comportamento aditivo — comandos existentes de Gabi não são alterados

---

## Notas Técnicas

**Arquivo a modificar:** `squads/la-bella/agents/gabi-social.md` (projeto aiox-core-main)

**Implementação:** Adicionar ao bloco YAML do agente Gabi:
1. Dois novos comandos no menu de saudação: `*buscar-produto {SKU}` e `*listar-produtos [--categoria=X]`
2. Comportamento no bloco `comportamentos` ou `commands`: definir lógica de chamada HTTP à API
3. A chave `GABI_API_KEY` pode vir de variável de ambiente ou ser hardcoded como constante no agente (sem expor ao usuário)

**Endpoint GET `/api/gabi/produto/[sku]`** retorna:
```json
{
  "sku": "LBG100IPANEMA",
  "nome_produto": "Cuba de Embutir Ipanema",
  "categoria": "cubas",
  "cor_dominante": "branco",
  "material_aparente": "cerâmica",
  "qualidade_foto": "boa",
  "image_url": "https://res.cloudinary.com/...",
  "descricao_marketing": "...",
  "tags": ["cuba", "embutir", "banheiro"],
  "imagens": [{ "id": 1, "image_url": "...", "angulo": "frontal", "qualidade_foto": "boa" }]
}
```

**Endpoint GET `/api/gabi/produtos?categoria=cuba`** retorna array de produtos.

---

## File List

| Arquivo | Projeto | Ação |
|---------|---------|------|
| `squads/la-bella/agents/gabi-social.md` | aiox-core-main | MODIFY — adicionar comandos *buscar-produto e *listar-produtos |
| `src/middleware.ts` | lbg-next | MODIFY — X-API-Key bypass para /api/gabi/* (bloqueador descoberto) |

---

## Definition of Done

- [ ] Gabi executa `*buscar-produto {SKU}` e retorna dados completos do lbg-next
- [ ] Gabi executa `*listar-produtos --categoria=cuba` e retorna lista filtrada
- [ ] Fallback de produto não encontrado funciona
- [ ] Fallback de API offline funciona
- [ ] Comandos existentes de Gabi não foram alterados
- [ ] Story 3.3 desbloqueada (image_url disponível para uso)
