# Story 0.3: Magic Link — Login sem Senha

**Epic:** EPIC-0 — Auth & Segurança
**Depende de:** Story 0.2 (Resend instalado + tabela `auth_tokens` criada)
**Status:** Ready for Review
**Criado em:** 2026-06-05
**Executor:** `@dev`
**Quality Gate:** `@qa`
**Quality Gate Tools:** `[auth_check, token_expiry, email_delivery, jwt_generation, typescript_strict]`

---

## Story

**As a** usuária cadastrada no sistema,
**I want** entrar com um link enviado por email sem precisar lembrar minha senha,
**so that** o acesso seja mais rápido e seguro no dia a dia.

---

## Acceptance Criteria

1. Botão "Entrar sem senha" visível na página `/login`, separado do form de senha
2. Ao clicar: exibe campo de email + botão "Enviar link mágico"
3. `POST /api/auth/magic-link` aceita `{ email }` — retorna sempre `200 { ok: true }` (não revela se email existe)
4. Se email existe em `usuarios` e está ativo: gerar token UUID, salvar em `auth_tokens` com `tipo='magic_link'`, `expira_em = NOW() + 15min`
5. Email enviado via Resend com link: `{NEXTAUTH_URL}/api/auth/magic-link?token={token}`
6. `GET /api/auth/magic-link?token={token}` valida o token
7. Token válido: buscar usuário por email, gerar JWT (8h), setar cookie `lbg_token`, marcar token como `usado = true`, redirecionar para `/catalogo`
8. Token inválido (expirado, já usado, inexistente): redirecionar para `/login?error=magic_link_invalido`
9. Página `/login` exibe mensagem de erro se `?error=magic_link_invalido`
10. Após envio do link: exibir mensagem "Link enviado! Verifique seu email. Expira em 15 minutos."
11. Token de magic link expira em 15 minutos (mais curto que reset de senha — 1h)
12. Apenas usuários com `ativo = true` recebem magic link
13. TypeScript compilando sem erros (`npx tsc --noEmit`)
14. ESLint sem erros (`npm run lint`)

---

## 🤖 CodeRabbit Integration

**Story Type Analysis:**
- **Primary Type:** Security (autenticação sem senha)
- **Secondary Type(s):** API, Email
- **Complexity:** Small-Medium — reutiliza infraestrutura da Story 0.2

**Specialized Agent Assignment:**

Primary Agents:
- `@dev` (implementação)
- `@qa` (validação de segurança — token de uso único, 15min)

**Quality Gate Tasks:**
- [ ] Pre-Commit (`@dev`): `npm run lint && npx tsc --noEmit`
- [ ] Segurança: token marcado `usado = true` ANTES de emitir o JWT (evitar race condition)
- [ ] Response de `magic-link` POST sempre 200 (não vazar existência de email)

**CodeRabbit Focus Areas:**

Primary Focus:
- Race condition: marcar token como usado ANTES de gerar JWT na resposta
- Redirect após login: usar `NextResponse.redirect` com cookie setado no header
- Token curto (15min): verificar `expira_em` com margem adequada

Secondary Focus:
- Cookie gerado com mesmos parâmetros do login normal (`httpOnly`, `secure`, `sameSite: lax`, `maxAge: 8h`)
- Não logar token em nenhum log de servidor

---

## Tasks / Subtasks

- [ ] **Task 1 — API `POST /api/auth/magic-link`** (AC: 3, 4, 5)
  - [ ] 1.1 Validar body: `email` presente e formato válido
  - [ ] 1.2 Buscar usuário em `usuarios` por email com `ativo = true` (silenciosamente)
  - [ ] 1.3 Se usuário existe: gerar token via `crypto.randomUUID()`
  - [ ] 1.4 INSERT em `auth_tokens`: `{ email, token, tipo: 'magic_link', expira_em: +15min }`
  - [ ] 1.5 Enviar email via Resend com link de magic link
  - [ ] 1.6 Retornar `200 { ok: true }` independente de o email existir

- [ ] **Task 2 — API `GET /api/auth/magic-link`** (AC: 6, 7, 8)
  - [ ] 2.1 Ler `token` da query string
  - [ ] 2.2 Buscar em `auth_tokens`: `token = ? AND tipo = 'magic_link' AND usado = false AND expira_em > NOW()`
  - [ ] 2.3 Se não encontrado → redirect para `/login?error=magic_link_invalido`
  - [ ] 2.4 Marcar `auth_tokens.usado = true` (ANTES de gerar JWT)
  - [ ] 2.5 Buscar usuário completo em `usuarios` por `tokenData.email`
  - [ ] 2.6 Gerar JWT com `SignJWT` — mesmos campos do login: `{ email, name, role }`
  - [ ] 2.7 Setar cookie `lbg_token` nos headers da resposta (mesmos params do login)
  - [ ] 2.8 Redirecionar para `/catalogo` via `NextResponse.redirect`

- [ ] **Task 3 — UI: botão "Entrar sem senha" no `/login`** (AC: 1, 2, 9, 10)
  - [ ] 3.1 Adicionar seção separada abaixo do form de senha em `src/app/login/page.tsx`
  - [ ] 3.2 Ao clicar em "Entrar sem senha": exibir campo email + botão "Enviar link mágico"
  - [ ] 3.3 Chamar `POST /api/auth/magic-link` no submit
  - [ ] 3.4 Após envio: exibir mensagem "Link enviado! Verifique seu email. Expira em 15 minutos."
  - [ ] 3.5 Se URL tem `?error=magic_link_invalido`: exibir "Link inválido ou expirado. Solicite um novo."

- [ ] **Task 4 — Middleware: rota pública** (AC: 6)
  - [ ] 4.1 Adicionar `/api/auth/magic-link` ao array `PUBLIC` no middleware
  - [ ] 4.2 (GET precisa ser público para processar o token sem cookie válido)

- [ ] **Task 5 — Quality Gates** (AC: 13, 14)
  - [ ] 5.1 `npx tsc --noEmit` — zero erros
  - [ ] 5.2 `npm run lint` — zero erros
  - [ ] 5.3 Testar fluxo completo: email → link → login automático → /catalogo
  - [ ] 5.4 Testar token expirado → redirect com error correto
  - [ ] 5.5 Testar reutilização de token → bloqueado (usado = true)
  - [ ] 5.6 Testar email não cadastrado → 200 sem email enviado

---

## Dev Notes

### Reutilização da Story 0.2

Esta story assume que estão **já instalados/criados:**
- `resend` (npm package)
- Tabela `auth_tokens` no Supabase
- Env vars `RESEND_API_KEY`, `RESEND_FROM_EMAIL`

**Não criar novamente** — o SQL da Story 0.2 já inclui `CHECK (tipo IN ('reset_senha', 'magic_link'))`. A tabela suporta magic_link sem alteração.

### Gerar JWT e setar cookie no GET handler

O desafio aqui é que o `GET` deve setar um cookie E fazer um redirect simultaneamente:

```typescript
import { SignJWT } from 'jose';
import { NextResponse } from 'next/server';

const SECRET = new TextEncoder().encode(process.env.NEXTAUTH_SECRET ?? 'change-me');

// Gerar token JWT
const jwtToken = await new SignJWT({ email: user.email, name: user.nome, role: user.role })
  .setProtectedHeader({ alg: 'HS256' })
  .setExpirationTime('8h')
  .sign(SECRET);

// Redirect com cookie
const response = NextResponse.redirect(new URL('/catalogo', process.env.NEXTAUTH_URL));
response.cookies.set('lbg_token', jwtToken, {
  httpOnly: true,
  secure: process.env.NODE_ENV === 'production',
  sameSite: 'lax',
  maxAge: 8 * 60 * 60,
  path: '/',
});
return response;
```

### Email de magic link

```typescript
await resend.emails.send({
  from: process.env.RESEND_FROM_EMAIL ?? 'noreply@lbg.com.br',
  to: email,
  subject: 'Seu link de acesso — La Bella Griffe',
  html: `
    <p>Olá,</p>
    <p>Clique no link abaixo para acessar o sistema. O link expira em <strong>15 minutos</strong>.</p>
    <a href="${process.env.NEXTAUTH_URL}/api/auth/magic-link?token=${token}">Acessar agora</a>
    <p>Se você não solicitou isso, ignore este email.</p>
  `,
});
```

### INSERT com 15 minutos de expiração

```typescript
const expiraEm = new Date(Date.now() + 15 * 60 * 1000).toISOString(); // +15min

await sb.from('auth_tokens').insert({
  email,
  token,
  tipo: 'magic_link',
  expira_em: expiraEm,
});
```

### Middleware — adicionar rotas públicas

```typescript
// Adicionar junto com as rotas da Story 0.2:
const PUBLIC = [
  '/login',
  '/api/auth/login',
  '/api/auth/reset-request',
  '/api/auth/reset-confirm',
  '/reset-password',
  '/api/auth/magic-link',   // ← esta story
];
```

### Arquivos a criar

```
src/app/api/auth/magic-link/route.ts    ← POST (enviar) + GET (validar + login)
```

### Arquivos a modificar

```
src/app/login/page.tsx     ← botão "Entrar sem senha" + mensagem de erro/sucesso
src/middleware.ts           ← adicionar /api/auth/magic-link ao PUBLIC
```

---

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-06-05 | 1.0 | Story criada a partir do EPIC-0 | River (@sm) |

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Debug Log References
Nenhum blocker.

### Completion Notes
- Race condition tratada: usado=true marcado ANTES de gerar JWT
- Reutiliza tabela auth_tokens da Story 0.2 (tipo='magic_link')
- GET e POST no mesmo arquivo route.ts (Next.js app router)

### File List
- `src/app/api/auth/magic-link/route.ts` (CREATED)
- `src/components/auth/LoginContent.tsx` (MODIFIED — botão "Entrar sem senha" + mensagem magic_link_invalido)

---

## QA Results

**Gate:** PASS
**Revisor:** Quinn (@qa) — 2026-06-05

**Verificações:**
- ✅ Race condition prevenida: `usado=true` marcado **antes** de gerar JWT
- ✅ Cookie com mesmos parâmetros do login normal (httpOnly, secure, sameSite:lax, maxAge:8h)
- ✅ Token não logado em nenhum log de servidor
- ✅ Anti-enumeration: POST sempre 200
- ✅ `GET /api/auth/magic-link` no PUBLIC do middleware — correto (precisa ser acessível sem cookie)
- ✅ Redirect + cookie set simultâneos via NextResponse.redirect + cookies.set
- ✅ Apenas usuários com `ativo=true` recebem o link
- ✅ tsc --noEmit: zero erros
- ✅ lint: zero erros novos
